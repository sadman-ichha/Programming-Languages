#include <stdio.h>

int main()
{
    int sadman = 20;
    int jone = 20;
    // This is if else statement
    if (sadman > jone)
    {
        printf("Sadman is Elder! \n");
    }
    else if (jone > sadman)
    {
        printf("Jone is elder!\n ");
    }
    else
    {
        printf("Nothing else\n");
    }
    return 0;
}