#include <stdio.h>

int main() {
    char myName[] = "Sadman " "Mahedi"; // Use an array to store a string
    char myLetter = 'D';

    printf("Hello, I'm %s\n", myName); // Use %s to print a string
    printf("Learning C language\n");
    return 0;
}
