#include <iostream>
using namespace std;

int main()
{
    int A, B, C, D, differnce;
    cin >> A >> B >> C >> D;
    differnce = (A * B) - (C * D);
    cout << "DIFERENCA = " << differnce << endl;

    return 0;
}